echo "Hello github actions from docker container"
echo "This is a bash script running inside a Docker container."
echo "Current date and time: $(date)"
echo "This script is running in the directory: $(pwd)"
echo "List of files in the current directory:"
ls -l
echo "End of script."
# This script prints a greeting message, the current date and time, the current directory, and lists the files in that directory.
# It serves as a simple demonstration of running a bash script inside a Docker container.
# It can be used for testing and automation purposes in a CI/CD pipeline.
# The script is executed when the Docker container is run, showcasing the functionality of the container.
# The script is executed when the Docker container is run, showcasing the functionality of the container.
# The script is executed when the Docker container is run, showcasing the functionality of the container.