name: main
run-name: playwright
on: [workflow_dispatch, push]
jobs:
    pages:
      uses: ./.github/workflows/playwright.yml
      with:
        project: chromium
        tests-dir: ./e2e-tests
        gh-page-branch: allure-report
        allure-results-dir: allure-results
