name: e2e tests
run-name: Rodion
on: [push]
jobs:
    tests:
        runs-on: ubuntu-latest
        steps:
          - name: test
            run: echo test
    fe:
      runs-on: ubuntu-latest
      steps:
        - name: lsing
          run: ls
        - name: checkout
          uses: actions/checkout@v4
        - name: cd to the fe dir
          run: cd fe
        - name: install deps
          run: npm ci
        - name: build fe app
          run: npm run build



