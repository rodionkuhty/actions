name: e2e tests
run-name: Rodion
on: [push]
jobs:
    tests:
        runs-on: ubuntu-latest
        steps:
          - name: test
            run: echo test
    fe:
      runs-on: ubuntu-latest
      steps:
        - name: lsing
          run: ls
